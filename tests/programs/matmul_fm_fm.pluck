; Pure-float matrix product. Exercises 2D x 2D matmul with no symbolic
; entries; result must be deterministic.
(query
  (let ((a {{1.0, 2.0}, {3.0, 4.0}})
        (b {{5.0, 6.0}, {7.0, 8.0}}))
    (Posterior (@ a b) True)))
