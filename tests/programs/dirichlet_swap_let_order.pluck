(define (swap-let-order)
    (let ((q (dirichlet 1.0 1.0))
          (p (dirichlet 1.0 1.0 1.0))
          (p0 (vector_index p @0))
          (q0 (vector_index q @0)))
    (Posterior (Pair p q) (and (flip p0) (flip q0)))))

(query swap-let-order (swap-let-order))
