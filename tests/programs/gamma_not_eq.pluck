; Excluding a point is measure-zero under the Gamma: the posterior
; equals the prior (the exclusion is tracked only to reject a later pin).
(query (let ((x (gamma 2.0 1.0))) (Posterior x (not (real_eq x 3.0)))))
