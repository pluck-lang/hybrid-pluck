; Same as poisson_eq but with a Peano nat literal comparand: the bare
; `3` parses to (S (S (S (O)))) and is forced to the integer 3.
(query (let ((r (gamma 2.0 1.0))) (Posterior r (native_eq (poisson r) 3))))
