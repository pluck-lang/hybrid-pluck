(define (dirichlet-prob-obs)
    (let ((p (dirichlet 1.0 1.0 1.0))
          (p1 (vector_index p @1)))
    (Posterior p (not (flip p1)))))

(query dirichlet-component-observation (dirichlet-prob-obs))